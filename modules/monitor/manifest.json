{
  "display_name": "Monitor",
  "tooltip": "Read-only manufacturing fleet monitor · live 20-machine OEE/availability/throughput/quality/health streaming from the fleet simulator, per-machine drill-down, and a gpt-5.4-mini Ask-AI that answers machine/fleet questions grounded in live data with the right chart. No actuation — the decision loop lives in the Optimize module.",
  "icon": "icon.svg",
  "dashboard": {
    "title": "Monitor · Fleet",
    "default_height": 960,
    "badge_color": "info"
  },
  "service": {
    "connector_url": "http://127.0.0.1:9310",
    "health_path": "/connector/health",
    "streaming": false,
    "min_core_version": "2",
    "tools": [
      { "name": "monitor_fleet", "description": "Read the live fleet: every machine's state/OEE/availability/health plus a summary and the at-risk scenario.", "parameters": { "type": "object", "properties": { "url": { "type": "string" }, "lang": { "type": "string" } } } },
      { "name": "monitor_machine", "description": "Read one machine's live telemetry.", "parameters": { "type": "object", "properties": { "machine_id": { "type": "string" }, "url": { "type": "string" }, "lang": { "type": "string" } }, "required": ["machine_id"] } },
      { "name": "monitor_ask", "description": "Answer a natural-language question about the fleet, grounded in live data, with the best-fit chart.", "parameters": { "type": "object", "properties": { "question": { "type": "string" }, "lang": { "type": "string" } }, "required": ["question"] } }
    ]
  },
  "activity": {
    "default": { "running": "Working…", "done": "Done" },
    "actions": {
      "status": { "running": "Reading fleet…", "done": "Live" },
      "ask":    { "running": "Asking the fleet…", "done": "Answered" }
    }
  },
  "subagent": { "enabled": false, "model": null, "tools": null }
}
