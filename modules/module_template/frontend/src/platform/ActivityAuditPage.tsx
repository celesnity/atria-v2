import type { AuditEntry } from './types';
import { Agent } from '../embinder';

export function ActivityAuditPage({ entries, onReset }: { entries: AuditEntry[]; onReset: () => void }) {
  return <div><p style={{ color: '#9ca8c8', fontSize: 12, letterSpacing: '0.1em', fontWeight: 800 }}>CHRONOLOGICAL PLATFORM TRACE</p><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', margin: '6px 0 18px' }}><h1 style={{ margin: 0, fontSize: 34 }}>Activity &amp; Audit</h1><Agent.Button name="reset_platform" description="Reset the deterministic platform demonstration" onAct={onReset} style={{ background: '#1b2540', border: '1px solid #2b3758', boxShadow: 'none' }}>Reset demo</Agent.Button></div><section style={{ border: '1px solid #2b3758', borderRadius: 12, background: '#141b31', overflow: 'hidden' }}>{entries.slice().reverse().map((entry) => <article key={entry.id} style={{ display: 'grid', gridTemplateColumns: '130px minmax(0, 1fr) auto', gap: 14, padding: 15, borderBottom: '1px solid #263251' }}><time style={{ color: '#9ca8c8', fontSize: 12 }}>{entry.at.slice(11, 16)} UTC</time><div><strong style={{ textTransform: 'capitalize' }}>{entry.action.replaceAll('_', ' ')}</strong><p style={{ color: '#9ca8c8', margin: '4px 0 0' }}>{entry.detail}</p>{entry.correlationId ? <code style={{ color: '#91b7ff', fontSize: 11 }}>{entry.correlationId}</code> : null}</div><span style={{ color: entry.status === 'failed' ? '#fb7185' : entry.status === 'pending' ? '#fbbf24' : '#34d399', fontWeight: 800, fontSize: 12 }}>{entry.status.toUpperCase()}</span></article>)}</section></div>;
}
