# fastapi + pydantic + the whole /connector/* contract come from minder-python-sdk (installed from the
# repo root in the Dockerfile). Optimize's engine (scripts/pipeline.py, simulate.py, ai.py, store.py) is
# stdlib-only (urllib for the fleet API + the LLM), so nothing else is needed.
uvicorn[standard]>=0.29
